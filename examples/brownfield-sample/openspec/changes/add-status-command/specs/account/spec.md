## ADDED Requirements

### Requirement: Status command
Operators SHALL be able to inspect account subsystem status from the CLI.

#### Scenario: Operator checks status
- **WHEN** an operator runs the account status command
- **THEN** the CLI SHALL report the account subsystem status
